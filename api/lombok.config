# This config stops lombok annotations from brining down test coverage results in Sonarcloud
# by excluding lombok generated methods
config.stopBubbling = true
lombok.addLombokGeneratedAnnotation = true